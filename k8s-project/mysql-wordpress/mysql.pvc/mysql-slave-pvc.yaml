apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mysql-slave-pvc
    namespace: wordpress-namespace
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 4Gi
